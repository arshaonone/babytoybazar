<div style="padding:4rem 0">
  <div class="container" style="max-width:860px">
    <h1 style="margin-bottom:2rem">{{ page.title }}</h1>
    <div style="line-height:1.9;color:var(--color-text-secondary)">{{ page.content }}</div>
  </div>
</div>
